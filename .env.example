# {{PROJECT_NAME}} - Environment Variables
# ============================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!

# ===========================================
# CREDENTIALS (SENSITIVE - Required)
# ===========================================

# Primary System Access
# Example: API credentials, database auth, external service login
# SYSTEM_USERNAME=your_username
# SYSTEM_PASSWORD=your_password
# SYSTEM_API_KEY=your_api_key

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================

# SMTP Settings for notifications/alerts
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_email_password
EMAIL_FROM=noreply@example.com
EMAIL_RECIPIENTS=recipient1@example.com,recipient2@example.com

# ===========================================
# PATHS (Adjustable)
# ===========================================

# Data storage paths (relative or absolute)
DATA_PATH=./data
LOGS_PATH=./logs
HISTORY_PATH=./data/history

# ===========================================
# APPLICATION SETTINGS
# ===========================================

# Debug mode (true/false)
DEBUG=false

# Retry configuration
MAX_RETRIES=3
TIMEOUT_SECONDS=30

# ===========================================
# OPTIONAL INTEGRATIONS
# ===========================================

# Slack notifications (if applicable)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Database connection (if applicable)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Cloud storage (if applicable)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# S3_BUCKET_NAME=your_bucket

# ===========================================
# NOTES
# ===========================================
#
# 1. All variables with actual values should be in .env (not committed)
# 2. This file (.env.example) is safe to commit as it has no real secrets
# 3. Use pydantic-settings to load these variables in Python:
#
#    from pydantic_settings import BaseSettings
#
#    class Settings(BaseSettings):
#        EMAIL_HOST: str
#        EMAIL_PORT: int = 587
#        DEBUG: bool = False
#
#        model_config = SettingsConfigDict(env_file=".env")
#
